# CodeLens Environment Configuration
# Copy this file to .env and configure as needed

# Frontend Configuration
# WebSocket server URL for backend connection
WEBSOCKET_SERVER_URL=ws://localhost:8000

# Backend Configuration
# Allowed CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000

# WebSocket ping interval in seconds
WEBSOCKET_PING_INTERVAL=30

# Test Runner WebSocket URL (internal docker network)
TEST_RUNNER_URL=ws://test-runner:8001

# Test Runner Configuration
# Kubernetes namespace for test execution
KUBERNETES_NAMESPACE=default

# Path to kubeconfig file (Docker Desktop)
# Windows: C:\Users\YourUser\.kube\config
# Unix/macOS: ~/.kube/config
KUBECONFIG_PATH=~/.kube/config
